name: MacOS AppleClang

on:
  push:
    branches: ["main"]
    paths-ignore: ["**/*.md", "docs/**"]
  pull_request:
    branches: ["main"]
    paths-ignore: ["**/*.md", "docs/**"]
  workflow_dispatch:

jobs:
  build:
    name: Build & Test (${{ matrix.tls.name }})
    runs-on: macos-latest
    timeout-minutes: 60

    strategy:
      fail-fast: false
      matrix:
        build_type: [Debug]
        std: [23]
        tls:
          - { name: No TLS,  backend: none }
          - { name: wolfSSL, backend: wolfssl, vcpkg_port: "wolfssl[asio]" }
          - { name: OpenSSL, backend: openssl, vcpkg_port: openssl }

    steps:
      - uses: actions/checkout@v6

      - name: Setup toolchain
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Toolchain info
        run: |
          xcodebuild -version
          which clang
          which clang++
          clang --version
          xcrun clang --version
          xcrun clang++ --version
          
      - name: Detect vcpkg triplet
        if: matrix.tls.vcpkg_port
        shell: bash
        run: |
          if [[ "$(uname -m)" == "arm64" ]]; then
            echo "VCPKG_DEFAULT_TRIPLET=arm64-osx" >> "$GITHUB_ENV"
          else
            echo "VCPKG_DEFAULT_TRIPLET=x64-osx" >> "$GITHUB_ENV"
          fi

      - name: Setup vcpkg
        if: matrix.tls.vcpkg_port
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgGitCommitId: 29f10019c26142cc4689b53b8c91da519db09e5c

      - name: Install ${{ matrix.tls.name }} (vcpkg)
        if: matrix.tls.vcpkg_port
        run: |
          "$VCPKG_ROOT/vcpkg" install "${{ matrix.tls.vcpkg_port }}" --triplet "${VCPKG_DEFAULT_TRIPLET}"

      - name: Configure CMake
        shell: bash
        run: |
          vcpkg_cmake_args=()
          if [[ -n "${{ matrix.tls.vcpkg_port }}" ]]; then
            vcpkg_cmake_args+=("-DCMAKE_TOOLCHAIN_FILE=${VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake")
            vcpkg_cmake_args+=("-DVCPKG_TARGET_TRIPLET=${VCPKG_DEFAULT_TRIPLET}")
          fi

          cmake -B "${{ github.workspace }}/build-${{ matrix.tls.backend }}" \
            -DCMAKE_CXX_STANDARD=${{ matrix.std }} \
            -DCMAKE_CXX_STANDARD_REQUIRED=ON \
            -DCMAKE_CXX_EXTENSIONS=OFF \
            -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
            -DAERO_TLS_BACKEND=${{ matrix.tls.backend }} \
            "${vcpkg_cmake_args[@]}"

      - name: Build
        run: cmake --build "${{ github.workspace }}/build-${{ matrix.tls.backend }}" --config ${{ matrix.build_type }} --parallel

      - name: Test
        working-directory: ${{ github.workspace }}/build-${{ matrix.tls.backend }}
        run: ctest --output-on-failure --timeout 30 -C ${{ matrix.build_type }}
